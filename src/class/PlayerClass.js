/** Player class */
export default class Player {
  constructor(mark, color, status, selected) {
    this.mark = mark;
    this.color = color;
    this.status = status;
    this.selected = selected;
  }
}
